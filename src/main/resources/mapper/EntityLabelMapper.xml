<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mobileproject.mapper.EntityLabelMapper">

    <resultMap id="LabelResult" type="org.example.mobileproject.entity.EntityLabel">
        <id property="id" column="id"/>
        <result property="labelName" column="label_name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectById" parameterType="long" resultMap="LabelResult">
        SELECT * FROM entity_labels WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="LabelResult">
        SELECT * FROM entity_labels ORDER BY id
    </select>

    <insert id="insert" parameterType="org.example.mobileproject.entity.EntityLabel" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO entity_labels (label_name, description, created_at)
        VALUES (#{labelName}, #{description}, NOW())
    </insert>

    <update id="update" parameterType="org.example.mobileproject.entity.EntityLabel">
        UPDATE entity_labels SET label_name=#{labelName}, description=#{description} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM entity_labels WHERE id = #{id}
    </delete>

</mapper>
